<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.1.xsd">
    <changeSet author="CredibleX" id="0038-insert-overdue-loans-table-report">
        <preConditions onFail="MARK_RAN" onError="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM stretchy_report
                WHERE report_name = 'Loan payments due (Overdue Loans) Table Report'
            </sqlCheck>
        </preConditions>

        <insert tableName="stretchy_report">
            <column name="report_name" value="Loan payments due (Overdue Loans) Table Report"/>
            <column name="report_type" value="Table"/>
            <column name="report_subtype" value="NULL"/>
            <column name="report_category" value="Loans"/>
            <column name="report_sql"><![CDATA[
SELECT mc.id AS id,
       mc.firstname AS firstName,
       mc.middlename AS middleName,
       mc.lastname AS lastName,
       mc.display_name AS fullName,
       mc.mobile_no AS mobileNo,
       ml.principal_amount AS loanAmount,
       (COALESCE(ml.principal_outstanding_derived, 0)
         + COALESCE(ml.interest_outstanding_derived, 0)
         + COALESCE(ml.fee_charges_outstanding_derived, 0)
         + COALESCE(ml.penalty_charges_outstanding_derived, 0)) AS loanOutstanding,
       ml.principal_disbursed_derived AS loanDisbursed,
       laa.overdue_since_date_derived AS paymentDueDate,
       (COALESCE(SUM(ls.principal_amount), 0) - COALESCE(SUM(ls.principal_writtenoff_derived), 0)
        + COALESCE(SUM(ls.interest_amount), 0) - COALESCE(SUM(ls.interest_writtenoff_derived), 0)
        - COALESCE(SUM(ls.interest_waived_derived), 0) + COALESCE(SUM(ls.fee_charges_amount), 0)
        - COALESCE(SUM(ls.fee_charges_writtenoff_derived), 0) - COALESCE(SUM(ls.fee_charges_waived_derived), 0)
        + COALESCE(SUM(ls.penalty_charges_amount), 0) - COALESCE(SUM(ls.penalty_charges_writtenoff_derived), 0)
        - COALESCE(SUM(ls.penalty_charges_waived_derived), 0)) AS totalDue,
       laa.total_overdue_derived AS totalOverdue,
       ounder.id AS officeNumber,
       ml.account_no AS loanAccountId,
       gua.lastname AS guarantorLastName,
       COUNT(gua.id) AS numberOfGuarantors,
       g.display_name AS groupName,
       (COALESCE(SUM(ls.penalty_charges_amount), 0)
        - COALESCE(SUM(ls.penalty_charges_writtenoff_derived), 0)
        - COALESCE(SUM(ls.penalty_charges_waived_derived), 0)) AS lateFee
FROM m_office mo
JOIN m_office ounder ON ounder.hierarchy LIKE CONCAT(mo.hierarchy, '%')
INNER JOIN m_client mc ON mc.office_id = ounder.id
INNER JOIN m_loan ml ON ml.client_id = mc.id
INNER JOIN r_enum_value rev ON rev.enum_id = ml.loan_status_id AND rev.enum_name = 'loan_status_id'
INNER JOIN m_loan_arrears_aging laa ON laa.loan_id = ml.id
LEFT JOIN m_loan_repayment_schedule ls ON ls.loan_id = ml.id
LEFT JOIN m_currency cur ON cur.code = ml.currency_code
LEFT JOIN m_group_client gc ON gc.client_id = mc.id
LEFT JOIN m_group g ON g.id = gc.group_id
LEFT JOIN m_staff lo ON lo.id = ml.loan_officer_id
LEFT JOIN m_guarantor gua ON gua.loan_id = ml.id
WHERE ml.loan_status_id = 300
  AND mo.id = '${officeId}'
  AND (COALESCE(ml.loan_officer_id, -10) = ${loanOfficerId} OR '-1' = ${loanOfficerId})
  AND (EXTRACT(DAY FROM (CURRENT_DATE - ls.duedate::TIMESTAMP)) BETWEEN ${fromX} AND ${toY})
  AND (EXTRACT(DAY FROM (CURRENT_DATE - laa.overdue_since_date_derived::TIMESTAMP)) BETWEEN ${overdueX} AND ${overdueY})
GROUP BY ml.id, mc.id, laa.loan_id, ounder.id, gua.id, g.id
ORDER BY ounder.hierarchy, ml.currency_code, mc.account_no, ml.account_no
            ]]></column>

            <column name="description" value=""/>
            <column name="core_report" valueBoolean="false"/>
            <column name="use_report" valueBoolean="false"/>
            <column name="self_service_user_report" valueBoolean="false"/>
        </insert>
    </changeSet>
    <changeSet author="CredibleX" id="0038-map-overdue-loans-table-report-params">
        <preConditions onFail="MARK_RAN" onError="MARK_RAN">
            <sqlCheck expectedResult="1">
                SELECT COUNT(*)
                FROM stretchy_report
                WHERE report_name = 'Loan payments due (Overdue Loans) Table Report'
            </sqlCheck>
        </preConditions>

        <sql><![CDATA[
INSERT INTO stretchy_report_parameter (report_id, parameter_id, report_parameter_name)
SELECT r.id, p.id, p.parameter_name
FROM stretchy_report r
JOIN stretchy_parameter p
    ON p.parameter_name IN ('loanOfficerIdSelectAll', 'OfficeIdSelectOne', 'fromXSelect', 'toYSelect', 'overdueXSelect', 'overdueYSelect')
WHERE r.report_name = 'Loan payments due (Overdue Loans) Table Report'
  AND NOT EXISTS (
      SELECT 1 FROM stretchy_report_parameter srp
      WHERE srp.report_id = r.id
        AND srp.parameter_id = p.id
  );
        ]]></sql>
    </changeSet>

</databaseChangeLog>
